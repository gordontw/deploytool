# deploy server
beta:
  Nodes:         [192.168.0.1, 192.168.0.2]
  Repository:    git@github.com:gordontw/test.git
  Srcpath:       /tmp/deploy
  Dstpath:       /home
  Deployflow:    [local.run1,remote.test]

# run task
TASKS:
  local.clone:       git clone 
  local.run1:        date; ls -la
  local.run2:        phpunit tests/
  remote.env:        export a=b
  remote.test:       phpunit tests/
